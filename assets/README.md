# Assets

Store static project assets here, such as images, templates, reference files, sample media, or other non-code resources.

Keep large binary files out of the repository unless they are required fixtures or documented project inputs.
