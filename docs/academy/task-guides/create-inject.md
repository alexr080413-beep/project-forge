# Create Inject

Use this guide to create an inject in the Inject Library.

## Steps

1. Open `Inject Library`.
2. Enter title, description, type, priority, and assigned controller.
3. Select `Create Inject`.
4. Confirm the dashboard, review queue, timeline, controller counts, and audit log update.

## Human Control Point

New injects should enter review before release.
