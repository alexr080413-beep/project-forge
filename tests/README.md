# Tests

This directory will contain the automated test suite for Project Forge.

Mirror the structure of `src/project_forge/` as production modules are added. Start with focused unit tests, then add integration tests when workflows cross module or system boundaries.

No behavior tests are included yet because Project Forge currently contains only a skeleton.
